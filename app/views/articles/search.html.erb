<section>
  <div class="search_list">
      <% if !@articles.blank? %>
          <ul class="list-group">
            <% @articles.each do |article| %>
                 <%= link_to article.title, article, class: "list-group-item list-group-item-action" %>
            <% end %>
          </ul>
      <% else %>
          <div class="jumbotron">
            <h3 class="header">oh, bro, you late one step.</h3>
          </div>
      <% end %>
    </div>
</section>
  
